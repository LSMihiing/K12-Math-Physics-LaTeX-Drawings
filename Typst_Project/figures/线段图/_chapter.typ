// ============================================================
// 数轴与线段 — 章节聚合文件
// ============================================================

#import "解不等式_数轴表示_基础.typ" as _不等式基础
#import "解不等式组_数轴表示.typ" as _不等式组
#import "解不等式组_交集数轴.typ" as _不等式交集

#let sep() = {
  v(2em)
  line(length: 100%, stroke: 0.5pt + luma(200))
  v(2em)
}

// ============================================================
= 数轴与线段
// ============================================================

== 不等式与数轴

=== 解不等式 · 数轴表示（基础） <案例_解不等式_数轴表示_基础>
#_不等式基础.render()
#sep()

=== 解不等式组 · 数轴表示
#_不等式组.render()
#sep()

=== 解不等式组 · 交集数轴
#_不等式交集.render()

#pagebreak()
