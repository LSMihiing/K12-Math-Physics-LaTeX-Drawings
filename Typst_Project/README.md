# K12 教辅绘图项目（Typst + CeTZ）

## 项目概述

使用 **Typst** + **CeTZ** 绘制 K12 教辅题目的配图与解答。与同级的 `LaTeX_Project/` 互补，提供更现代化的排版体验。

## 目录结构

```
Typst_Project/
├── main.typ                    # 主编译入口（含三级目录索引）
├── lib/                        # 公共库
│   ├── styles.typ              # 全局样式（字体、页面、题目/解答格式）
│   ├── grid-utils.typ          # 方格纸 & 坐标系工具函数
│   └── geo-utils.typ           # 几何图形工具（三角形、标注等）
├── figures/                    # 绘图+解答源文件（按绘图类型分目录）
│   ├── 几何作图/     (15 个)    # 尺规作图、图形变换、拼接、分数涂色
│   ├── 统计图表/     (6 个)     # 折线图、柱状图、饼图、直方图
│   ├── 线段图/       (3 个)     # 数轴、不等式表示
│   ├── 物理/         (20 个)    # 受力分析、压力、电磁学、电路
│   └── 数据表格/     (预留)     # 填表题
├── output/                     # PDF 输出（git 忽略）
├── .gitignore
└── README.md
```

**文件总数**：44 个绘图文件

## 技术栈

| 组件 | 版本 | 用途 |
|------|------|------|
| Typst | 0.14.2 | 排版引擎 |
| CeTZ | 0.4.2 | 矢量绘图（类 TikZ） |

## 编译方法

```bash
cd Typst_Project

# 一次性编译
typst compile main.typ output/main.pdf

# 实时预览（文件修改后自动重新编译）
typst watch main.typ output/main.pdf
```

## 目录索引

PDF 编译后自动生成三级目录，每个绘图都有独立条目，可直接点击跳转：

```
= 几何作图              ← 一级标题（学科/大类）
  == 尺规作图            ← 二级标题（绘图子分类）
    === 尺规取等长线段    ← 三级标题（具体绘图，可跳转）
    === 尺规作图 · 角平分线与垂线
  == 图形变换
    === 格点三角形中心对称
    ...
```

## 添加新题目

1. 在 `figures/` 对应类型子目录下新建 `.typ` 文件
2. **文件名为纯描述性**，不使用题号（如 `尺规取等长线段.typ`）
3. 文件中定义 `render()` 函数，包含绘图和解答
4. 在 `main.typ` 中：
   - 添加 `#import` 语句
   - 添加 `=== 描述性标题`（三级标题）
   - 调用 `#模块名.render()`
   - 绘图之间使用 `#sep()` 分隔

### 文件模板

```typst
// ============================================================
// {描述性名称}
// ============================================================
#import "@preview/cetz:0.4.2"
#import "../../lib/grid-utils.typ": draw-grid, dot-label

#let render() = {
  // 题目文字 ...
  // CeTZ 绘图 ...
  // 解答文字 ...
}
```

### 命名规范

| 分类 | 命名示例 |
|------|---------|
| 几何作图 | `尺规取等长线段.typ`、`三角形平移旋转.typ` |
| 统计图表 | `北京南京气温_复式折线统计图.typ` |
| 物理受力 | `受力_斜面物体与绳子.typ` |
| 物理压力 | `压力_水平竖直斜面.typ` |
| 物理电路 | `电路图_电动机滑动变阻器.typ` |

## 与 LaTeX 项目的关系

| | LaTeX_Project | Typst_Project |
|---|---|---|
| 引擎 | XeLaTeX | Typst |
| 绘图库 | TikZ / PGF | CeTZ |
| 现有规模 | 315 tikzpicture + 1155 参考图 | 44 个绘图文件 |
| 适用场景 | 大规模存量图迁移 | 新题绘制 |
