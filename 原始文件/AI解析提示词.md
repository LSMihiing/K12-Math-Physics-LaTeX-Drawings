## 答案格式化提示词
扫描识别答案图片，进行格式化输出：

1. 公式的识别结果应为latex公式，代码用单美元符号$包裹。允许使用aligned环境，进行公式对齐。但是aligned代码整体仍为一行，不进行$内换行。例如：$\begin{aligned}= &(49 + 53 + 48 + 51 + 49) \div 5\\ = &(102 + 48 + 51 + 49) \div 5 \\= & (150 + 51 + 49) \div 5 \\= & (201 + 49) \div 5 \\= & 250 \div 5 \\= & 50 \text{（千克）}\end{aligned}$

2. 删除答案中的多余空行。

3. 将所有数字、字母用单美元符号$包裹。

4. 应该是多个单行的公式，不要错误识别为单行长公式。

5. 输出为latex代码框，方便我整体复制粘贴。

6. 只解析题目图片中的答案部分，忽略题干。删除答案中的“答案：解：”字样。

7. 原答案中明确需要换行分段的内容，使用空行进行强制分割。

8. 原题图无答案，请你帮我生成答案。原题图有答案，但是答案超过该题目所在学段的范围，需要你重新生成。

9. 删去答案中的数字序号“1. ”“2. ”“综上”等非小学数学解题习惯的术语。计算题答案尽可能地简便，删去答案中多余的语言表述，只列出核心的算式和“答：……”。

## 答案OCR结果格式化提示词

这是一本初中数学试题答案的OCR结果，请你帮我进行格式化。方便我录入系统。详细要求如下：

1. 公式的识别结果应为latex公式，代码用单美元符号$包裹。允许使用aligned环境，进行公式对齐。但是aligned代码整体仍为一行，不进行$内换行。例如：$\begin{aligned}= &(49 + 53 + 48 + 51 + 49) \div 5\\ = &(102 + 48 + 51 + 49) \div 5 \\= & (150 + 51 + 49) \div 5 \\= & (201 + 49) \div 5 \\= & 250 \div 5 \\= & 50 \text{（千克）}\end{aligned}$
2. 删除答案中的多余空行。
3. 将所有答案的数字、字母用单美元符号$包裹。
4. 应该是多个单行的公式，被错误识别为单行长公式。修正为单行公式。
5. 题号与答案内容分行
6. 多空答案，每空答案之间用6个半角空格分隔
7. 题目中含有小题号（1）（2）……不同小题间分行,且小题号所在行与大题行分行。小题号使用中文半角括号，即（1）（2）（3）
8. 选择题的答案A、B、C、D，统一修改为$\mathrm{A}$、$\mathrm{B}$、$\mathrm{C}$、$\mathrm{D}$
9. 大题的答案，根据内容合理分行，例如
```markdown 
（1）如图 ① ，延长 $AC$ 到 $F$ ，使 $CF = AP$ ，过点 $P$ 作 $PE // AB$ ，且 $PE = AB$ ，连接 $EF$ ，得到平移后的 $\triangle PEF$
（2）如图 ② ，由平移的性质得： $AB = PE$ ， $BC = EF$ ， $AC = PF$ ， $\angle B = \angle E = 90^{\circ}$ ， $\therefore S_{\triangle ABC} = S_{\triangle PEF}$ ， $S_{\triangle ABC} = S_{\text{四边形}ABOP} + S_{\triangle POQ}$ ， $\therefore S_{\text{四边形}COEF} = S_{\text{四边形}ABOP} = 22$
（3）由平移的性质得： $AB = PE$ ， $BC = EF$ ， $AC = PF$ ， $\angle B = \angle E = 90^{\circ}$ ， $BC // EF$ ， $AB // PE$ ， $\therefore$ 四边形ABOP与四边形COEF都是直角梯形。 $\because OC - BC - OB = a - 3$ ， $EF = BC = a$ ， $S_{\text{四边形}COEF} = \frac{1}{2} (OC + EF)\times OE = \frac{1}{2}\times (a - 3 + a)\times 2 = 2a - 3$ ，由（2）得： $S_{\text{四边形}COEF} = S_{\text{四边形}ABOP}$ ， $\therefore$ 四边形ABOP的面积为 $2a - 3$
```
需要分段为：
```markdown
（1）如图 ① ，
延长 $AC$ 到 $F$ ，使 $CF = AP$ ，
过点 $P$ 作 $PE // AB$ ，且 $PE = AB$ ，
连接 $EF$ ，
得到平移后的 $\triangle PEF$
（2）如图 ② ，
由平移的性质得： $AB = PE$ ， $BC = EF$ ， $AC = PF$ ， $\angle B = \angle E = 90^{\circ}$ ，
$\therefore S_{\triangle ABC} = S_{\triangle PEF}$ ， $S_{\triangle ABC} = S_{\text{四边形}ABOP} + S_{\triangle POQ}$ ，
$\therefore S_{\text{四边形}COEF} = S_{\text{四边形}ABOP} = 22$
（3）由平移的性质得： $AB = PE$ ， $BC = EF$ ， $AC = PF$ ， $\angle B = \angle E = 90^{\circ}$ ， $BC // EF$ ， $AB // PE$ ，
$\therefore$ 四边形ABOP与四边形COEF都是直角梯形。
$\because OC - BC - OB = a - 3$ ， $EF = BC = a$ ，$S_{\text{四边形}COEF} = \frac{1}{2} (OC + EF)\times OE = \frac{1}{2}\times (a - 3 + a)\times 2 = 2a - 3$ ，
由（2）得： $S_{\text{四边形}COEF} = S_{\text{四边形}ABOP}$ ，
$\therefore$ 四边形ABOP的面积为 $2a - 3$
```

例如：7.1节的答案应该被格式化为如下内容:

```markdown
## 7.1 同底数幂的乘法

### 【问题导引】

可进行 $10^{18}$ 次运算

### 【典型例题】

例解
（1）$(-5)^{3}\times 5^{3} = -5^{3}\times 5^{3} = -5^{6}$
（2）$3^{4}\times 9\times 81 = 3^{4}\times 3^{2}\times 3^{4} = 3^{4 + 2 + 4} = 3^{10}$
（3）$(x - 2y)^{2}\bullet (2y - x)^{3} = (2y-$ $x)^{2}\bullet (2y - x)^{3} = (2y - x)^{5}$
（4）$a^2\cdot (-a)^6 +a^3\cdot (-a)^5 = a^2\cdot a^6 -a^3\cdot a^5 = a^8 -a^8 = 0$


### 【活动与评估】

1. 
（1）不变，相加
（2）$a^6$
（3）$a^5$
（4）$5$
（5）$6$
2. 
（1）$\mathrm{B}$
（2）$\mathrm{C}$
（3）$\mathrm{A}$
（4）$\mathrm{A}$
3. 
（1）$128$
（2）$-128$
（3）$-b^9$
（4）$b^9$
（5）$5^6$
（6）$-b^6$
4. 
（1）$(a + b)^5$
（2）$-(a - b)^5$ [或 $(b - a)^5$]
（3）$-2t^3$
（4）$2a^{m+3}$
5. 
$S = 8.4 \times 10^8 (\mathrm{cm}^2)$ , $C = 1.24 \times 10^5 (\mathrm{cm})$
6. 
256
7. 
（1）方法一： $a^m \cdot a^n \cdot a^p = a^{m+n} \cdot a^p = a^{m+n+p}$
方法二： $a^m \cdot a^n \cdot a^p = \underbrace{(a \cdot a \cdots \cdot a)}_{m \text{个} a} \cdot \underbrace{(a \cdot a \cdots \cdot a)}_{n \text{个} a} \cdot \underbrace{(a \cdot a \cdots \cdot a)}_{p \text{个} a} = a^{m+n+p}$
（2）$a^m \cdot a^n \cdot a^p \cdots \cdot a^q = a^{m+n+p+\cdots+q}$（$m, n, p, q$ 是正整数）
```

## 作图题提示词

### 有答案

请你绘制这一题的答案。第一张图片为题干图片，第二张图片为参考答案图片。
1. 解题前，先详细分析题目（尤其是题干中的图片）。

2. 请你详细解析题目，根据解析，给出详细的绘图步骤，再生成latex绘图代码进行绘图。在latex代码中，给出绘图的详细注释。

3. 请你绘制的图表，风格、颜色尽量与原题干图表风格保持一致。

4. 如果涉及统计图的绘制，如果原图中的统计图有网格，请你在绘图中绘制网格，并且与统计图像正确对照。

5. 涉及表格的绘制，请你注意表格框线的有无、宽度等参数。可以参考前面题目的样式

6. 如果题目中涉及长度，请你根据线段的真实长度，按比例缩放，计算相关图像坐标。确保比例正确。

7. 将绘图的基本原理和基本步骤写入文档，放在每道题的解析后。方便我以后进行维护修改。绘画原理、步骤部分，要求语言简洁明确，不含多余文字、感情色彩。

不用进行命令行编译，我有自动编译。如果我的编译发现有报错，会提醒你进行命令行编译调试bug。


### 无答案

请你绘制这一题的答案。图片为题干图片。
1. 解题前，先详细分析题目（尤其是题干中的图片）。

2. 请你详细解析题目，根据解析，给出详细的绘图步骤，再生成latex绘图代码进行绘图。在latex代码中，给出绘图的详细注释。

3. 请你绘制的图表，风格、颜色尽量与原题干图表风格保持一致。

4. 如果涉及统计图的绘制，如果原图中的统计图有网格，请你在绘图中绘制网格，并且与统计图像正确对照。

5. 涉及表格的绘制，请你注意表格框线的有无、宽度等参数。可以参考前面题目的样式

6. 如果题目中涉及长度，请你根据线段的真实长度，按比例缩放，计算相关图像坐标。确保比例正确。

7. 将绘图的基本原理和基本步骤写入文档，放在每道题的解析后。方便我以后进行维护修改。绘画原理、步骤部分，要求语言简洁明确，不含多余文字、感情色彩。

不用进行命令行编译，我有自动编译。如果我的编译发现有报错，会提醒你进行命令行编译调试bug。
